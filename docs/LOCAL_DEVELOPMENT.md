# Local Development

1. Run `npm run bootstrap` to install dependencies.
2. Run `npm run doctor` to check your environment.
3. Start the dev server: `npm run dev` in `frontend/`.
4. Seed your dev environment: `npm run seed`.
5. Reset your dev environment: `npm run reset`.
